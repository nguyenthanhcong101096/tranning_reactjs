import * as action from '../constants/types';

export const CHANGE_USER = (user) => ({ type: action.CHANGE_USER, user })
export const CHANGE_COMPANY = (company) => ({ type: action.CHANGE_COMPANY, company })
